# Database configuration
# For local Docker development, use DB_HOST=mysql (service name in docker-compose.yml)
# For Azure VM, set DB_HOST to your VM IP address
DB_HOST=mysql
DB_USER=root
DB_PASSWORD=rootpassword
DB_NAME=yovi_db
DB_PORT=3306
